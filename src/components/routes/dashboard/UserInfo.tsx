import React from "react";

type Props = {};

const UserInfo = (props: Props) => {
  return (
    <main className="h-full w-[calc(100%-240px]">
      <div>Main</div>
    </main>
  );
};

export default UserInfo;
